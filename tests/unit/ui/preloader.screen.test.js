// import renderer from 'react-test-renderer';
// //import Preloader from '../../../src/components/ui/Preloader';

// it('renders correctly', () => {
//     const tree = renderer
//       .create(<a href={'https://jestjs.io/ru/docs/snapshot-testing'}>Some link</a>)
//       .toJSON();
//     expect(tree).toMatchSnapshot();
//   })

test('it should be screen test, but ...', () => {
    expect(1).toBe(1);
})